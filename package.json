{
  "name": "campus-issue-reporting-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx serve . -l 3000",
    "build": "echo 'No build step required'",
    "start": "npx serve . -l 3000",
    "lint": "echo 'No linting configured'"
  }
}
